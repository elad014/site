version: '3.8'

services:
  stocks-app:
    image: stock-app:latest
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1

  #########################################################